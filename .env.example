# Required
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx
GITHUB_USERNAME=your-github-login

# Optional — filter to a specific repo (org/repo). Leave blank for all repos.
GITHUB_REPO=mobly-app/monorepo

# Optional (defaults shown)
POLL_INTERVAL_MS=60000
PORT=3000

# Resend API key (resend.com — free tier, no SMTP hassle)
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# Optional: verified sending address (default works on free tier)
# EMAIL_FROM=PR Dashboard <you@yourdomain.com>

# Where to send reviews
REVIEW_TO_EMAIL=cameron@getmobly.com

# Mac review server URL (run ~/review-server.js on your Mac first)
MAC_REVIEW_URL=http://camerons-macbook-pro.local:3002/review
